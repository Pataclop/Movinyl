version: '3.8'

services:
  movinyl:
    build:
      context: .
      dockerfile: Dockerfile
      platforms:
        - linux/amd64
        - linux/arm64
    container_name: movinyl
    volumes:
      # Mount input videos directory
      - ./PROCESSING_ZONE:/app/PROCESSING_ZONE:rw
      # Mount output pages directory
      - ./PAGE_ZONE:/app/PAGE_ZONE:rw
      # Optional: mount a directory with videos from host
      - ${MOVIES_DIR:-./videos}:/app/videos:ro
    environment:
      - PYTHONUNBUFFERED=1
    working_dir: /app
    # Override with e.g. `disk` or `page` (ENTRYPOINT is `python3 -m movinyl`).
    command: doctor
    # For interactive use
    stdin_open: true
    tty: true

